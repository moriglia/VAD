Compile the pdf with one of the following commands
```bash
make -B

make -B report.pdf
```
Note that the `-B` option is required only from the second run,
since the target will appear up to date (if the previous compilation
was successful).
